To run the server first intall zig 0.14.0.
Then hit zig build run and send some network packets. 
if y want to set it run zig test and choose file but in zig it defines from which file to start testing.

